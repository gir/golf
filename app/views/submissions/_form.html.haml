%p{:style=>'color: red'}= flash[:error]
= error_messages_for :user
%p
  = select("language", "id", languages.map { |l| [l.name, l.id] }, { :prompt => "-- Select a language --" })
%p
  = f.label :code
  %br
  = f.text_area :code
