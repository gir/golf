#content
  %h1 Editing language

  - form_for(@language) do |f|
    = f.error_messages

    %p
      = f.label :name
      %br
      = f.text_field :name

    %p
      = f.submit 'Update'

  = link_to 'Show', @language
  &nbsp;|&nbsp;
  = link_to 'Back', languages_path
